{
  "name": "engine",
  "version": "1.0.0",
  "description": "Service where mostly computation task done by Redis queue and return the result in realtime",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/redis": "^4.0.11"
  }
}
